const fs = require("fs"); const path = require("path"); const pdf = require("pdf-parse"); async function test() { const inputDir = path.join(__dirname, "..", "knowledge_data", "【02】倪海厦文档全集-可打印（24本）"); console.log("测试目录:", inputDir); try { const files = await fs.promises.readdir(inputDir); const pdfFiles = files.filter(f => f.endsWith(".pdf")); console.log("找到PDF文件:", pdfFiles.length); } catch(e) { console.error("错误:", e.message); } } test();
